### 📌 À faire  :

   
####  Rends-le exécutable :

```bash
chmod +x generate_context.sh
```

#### Lance-le :

```bash
./generate_context.sh
```
 Il va générer un fichier context.txt avec :

- l’arborescence (sans venv, __pycache__, etc.)

-  les fichiers clés (app.py, requirements.txt, etc.)

-   un aperçu du contenu des fichiers .py (sauf ceux du venv)

